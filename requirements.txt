# Runtime dependencies. Single source of truth: pyproject.toml reads this file
# via [tool.setuptools.dynamic]. Keep comments on their own lines — setuptools
# does not strip trailing comments from requirement lines.
#
# Test/dev dependencies live in requirements-dev.txt.
# Exact pins: requirements.lock (regenerate with pip-compile).
# Host bootstrap: ./scripts/bootstrap-venv.sh — see docs/DEPENDENCIES.md.

# --- Core agent stack ---
langgraph>=0.2
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3

# --- Pluggable LLM backends (see app/llm.py) ---
# ollama (default, on-prem)
langchain-ollama>=0.2
# openai + OpenAI-compatible APIs (DeepSeek, etc.)
langchain-openai>=0.2
# bedrock_converse + bedrock embeddings
langchain-aws>=0.2
# anthropic
langchain-anthropic>=0.2
# google_genai
langchain-google-genai>=2

# --- RAG / vector store (optional, gated by AGENT_RAG_ENABLED) ---
langchain-chroma>=0.1
chromadb>=0.5

# --- Observability data sources ---
# PromQL against the Prometheus HTTP API
prometheus-api-client>=0.5
# async HTTP for Loki + Grafana
httpx>=0.27
requests>=2.31

# --- Trigger server + config ---
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0
