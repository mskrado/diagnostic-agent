# --- Core agent stack ---
langgraph>=0.2
langchain>=0.3
langchain-core>=0.3
langchain-community>=0.3

# --- Pluggable LLM backends (see app/llm.py) ---
langchain-ollama>=0.2          # ChatOllama + OllamaEmbeddings (default, on-prem)
langchain-openai>=0.2          # ChatOpenAI + OpenAIEmbeddings (fallback, reuses OPENAI_API_KEY)

# --- RAG / vector store (optional, gated by RAG_ENABLED) ---
langchain-chroma>=0.1
chromadb>=0.5

# --- Observability data sources ---
prometheus-api-client>=0.5     # PromQL against Prometheus HTTP API
httpx>=0.27                    # async HTTP for Loki + Grafana
requests>=2.31

# --- Trigger server + config ---
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.7
pydantic-settings>=2.3
pyyaml>=6.0

# --- Tests ---
pytest>=8.2
pytest-asyncio>=0.23
respx>=0.21                    # httpx mocking
